/**
 * DEMO ADMIN PANEL CONFIGURATION
 * -------------------------------
 * Frontend-only demo control panel (Ctrl + Shift + J).
 * The password below is intentionally hardcoded for demo purposes.
 *
 * ⚠ This is NOT secure authentication. Anyone can read this value in the
 * bundled JavaScript. Never store real secrets or sensitive data here.
 * Change this string to any value you prefer.
 */
export const ADMIN_PASSWORD = 'Lizz@3119'
